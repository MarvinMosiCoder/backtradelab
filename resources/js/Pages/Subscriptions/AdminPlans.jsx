import React, { useEffect, useState } from 'react';
import axios from 'axios';
import { Head, Link } from '@inertiajs/react';
import { ArrowDown, ArrowUp, Plus, Trash2 } from 'lucide-react';

export default function AdminPlans() {
  const [plans, setPlans] = useState([]), [loading, setLoading] = useState(true), [saving, setSaving] = useState(false), [status, setStatus] = useState('');
  useEffect(() => { let cancelled = false; axios.get('/subscription-plans').then(response => !cancelled && setPlans(response.data?.plans || [])).catch(() => !cancelled && setStatus('Unable to load plans.')).finally(() => !cancelled && setLoading(false)); return () => { cancelled = true; }; }, []);
  const update = (id, key, value) => setPlans(current => current.map(plan => plan.id === id ? { ...plan, [key]: value } : plan));
  const updateFeature = (planId, index, value) => setPlans(current => current.map(plan => plan.id === planId ? { ...plan, features: (plan.features ?? []).map((feature, featureIndex) => featureIndex === index ? value : feature) } : plan));
  const addFeature = planId => setPlans(current => current.map(plan => plan.id === planId && (plan.features ?? []).length < 8 ? { ...plan, features: [...(plan.features ?? []), ''] } : plan));
  const removeFeature = (planId, index) => setPlans(current => current.map(plan => plan.id === planId ? { ...plan, features: (plan.features ?? []).filter((_, featureIndex) => featureIndex !== index) } : plan));
  const moveFeature = (planId, index, direction) => setPlans(current => current.map(plan => {
    if (plan.id !== planId) return plan;
    const features = [...(plan.features ?? [])], next = index + direction;
    if (next < 0 || next >= features.length) return plan;
    [features[index], features[next]] = [features[next], features[index]];
    return { ...plan, features };
  }));
  const save = async () => { setSaving(true); setStatus(''); try { const response = await axios.put('/admin/subscription-plans', { plans: plans.map(({ id, price, duration_days, description, features, is_featured, is_active }) => ({ id, price: price === '' ? null : price, duration_days, description, features: (features ?? []).map(item => item.trim()).filter(Boolean), is_featured, is_active })) }); setPlans(response.data.plans); setStatus('Subscription pricing saved successfully.'); } catch (error) { setStatus(error.response?.data?.message || 'Unable to save plans.'); } finally { setSaving(false); } };
  return <><Head title="Subscription pricing"/><div className="space-y-5"><div className="flex flex-col justify-between gap-3 sm:flex-row sm:items-end"><div><h1 className="text-xl font-bold">Subscription pricing</h1><p className="mt-1 text-sm text-[#787b86]">Control server-priced plans and their displayed feature lists.</p></div><div className="flex gap-2"><Link href="/admin/subscriptions" className="flex h-10 items-center rounded-lg border border-[#2a2e39] px-4 text-sm font-semibold">Transactions</Link><button onClick={save} disabled={saving || loading} className="h-10 rounded-lg bg-[#2962ff] px-4 text-sm font-bold text-white disabled:opacity-50">{saving ? 'Saving…' : 'Save pricing'}</button></div></div><div className="grid gap-3 lg:grid-cols-3">{plans.map(plan => <article key={plan.id} className="rounded-xl border border-[#2a2e39] bg-[#131722] p-4 text-white"><div className="flex items-center justify-between"><div><div className="text-[10px] font-bold uppercase tracking-wider text-[#5b8cff]">{plan.code}</div><h2 className="text-lg font-bold">{plan.name}</h2></div><label className="flex items-center gap-2 text-xs text-[#b2b5be]"><input type="checkbox" checked={plan.is_active} onChange={event => update(plan.id, 'is_active', event.target.checked)}/>Active</label></div><div className="mt-3 grid gap-3"><Field label="Price (PHP)"><input type="number" min="0.01" step="0.01" value={plan.price ?? ''} onChange={event => update(plan.id, 'price', event.target.value)} placeholder="Set a price"/></Field><Field label="Replay access days"><input type="number" min="1" max="3650" value={plan.duration_days} onChange={event => update(plan.id, 'duration_days', Number(event.target.value))}/></Field><Field label="Plan description"><input maxLength="160" value={plan.description ?? ''} onChange={event => update(plan.id, 'description', event.target.value)}/></Field><div><div className="mb-1.5 flex items-center justify-between text-xs font-semibold text-[#787b86]"><span>Features ({(plan.features ?? []).length}/8)</span><button type="button" onClick={() => addFeature(plan.id)} disabled={(plan.features ?? []).length >= 8} className="flex items-center gap-1 text-[#5b8cff] disabled:opacity-40"><Plus size={13}/>Add</button></div><div className="grid gap-1.5">{(plan.features ?? []).map((feature, index) => <div key={`${plan.id}:${index}`} className="flex items-center gap-1"><input maxLength="80" value={feature} onChange={event => updateFeature(plan.id, index, event.target.value)} className="h-9 min-w-0 flex-1 rounded-lg border border-[#2a2e39] bg-[#0b0e14] px-2 text-xs outline-none focus:border-[#2962ff]"/><button type="button" disabled={index === 0} onClick={() => moveFeature(plan.id, index, -1)} className="p-1 disabled:opacity-30" aria-label="Move feature up"><ArrowUp size={13}/></button><button type="button" disabled={index === (plan.features ?? []).length - 1} onClick={() => moveFeature(plan.id, index, 1)} className="p-1 disabled:opacity-30" aria-label="Move feature down"><ArrowDown size={13}/></button><button type="button" onClick={() => removeFeature(plan.id, index)} className="p-1 text-red-400" aria-label="Remove feature"><Trash2 size={13}/></button></div>)}</div></div><label className="flex items-center gap-2 text-xs text-[#b2b5be]"><input type="checkbox" checked={plan.is_featured} onChange={event => update(plan.id, 'is_featured', event.target.checked)}/>Highlight as popular</label></div></article>)}</div>{loading && <div className="py-8 text-center text-[#787b86]">Loading plan settings…</div>}{status && <div className="rounded-lg border border-[#2a2e39] bg-[#131722] p-3 text-sm text-blue-300">{status}</div>}</div></>;
}

function Field({ label, children }) { return <label className="grid gap-1.5 text-xs font-semibold text-[#787b86]">{label}{React.cloneElement(children, { className: 'h-10 rounded-lg border border-[#2a2e39] bg-[#0b0e14] px-3 text-sm text-white outline-none focus:border-[#2962ff]' })}</label>; }
